<h1>All notes</h1>
<%= render 'search' %>

<% @notes.each do |note| %>
  <p><%= link_to note.name, edit_note_path(note) %></p>
  <p><%= note.content %></p>
  <br>
<% end %>

<h2>Create a new note</h2>
<%= render "form", note: @note %>
