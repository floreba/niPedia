<%= simple_form_for tagging do |f| %>
  <%= f.input :name ,input_html: { class: "input-field" } %>
  <%= f.hidden_field :tagger_id, value: @note.id %>
  <%= f.button :submit, "Create Tagging" %>
<% end %>
