<div class="container">
  <h2 class="header">Your Folders</h2>

  <% @folders.each do |folder| %>
    <p><%= link_to folder.name, folder_path(folder) %></p>
  <% end %>

  <h2>Add a folder</h2>

  <%= simple_form_for [@folder] do |f| %>
    <div class="m-3">
      <%= f.input :name, placeholder: "new folder" %>
      <%# <%= f.button :submit %>
    </div>
  <% end %>
</div>
